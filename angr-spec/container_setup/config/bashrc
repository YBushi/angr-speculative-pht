# Only run in interactive shells
[ -z "$PS1" ] && return

# Auto-activate angr7 if it exists
VENV="$HOME/.virtualenvs/angr7"
if [ -f "$VENV/bin/activate" ]; then
  source "$VENV/bin/activate"
  echo "✅ angr7 activated from .bashrc"
fi
